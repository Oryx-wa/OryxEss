/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {SecurityService2} from './security-service2';

describe('SecurityService2', () => {
  it('should create an instance', () => {
    expect(new SecurityService2()).toBeTruthy();
  });
});
