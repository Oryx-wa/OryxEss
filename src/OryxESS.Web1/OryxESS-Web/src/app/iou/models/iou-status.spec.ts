import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {IouStatus} from './iou-status';

describe('IouStatus', () => {
  it('should create an instance', () => {
    expect(new IouStatus()).toBeTruthy();
  });
});
