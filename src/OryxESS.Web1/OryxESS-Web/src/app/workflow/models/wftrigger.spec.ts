import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {WFTrigger} from './wftrigger';

describe('WFTrigger', () => {
  it('should create an instance', () => {
    expect(new WFTrigger()).toBeTruthy();
  });
});
